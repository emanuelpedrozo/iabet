'use client'; import {useState} from 'react'; import {API} from '@/lib/api';
export default function Login(){const [error,setError]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`${API}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(!r.ok)return setError('Credenciais inválidas');const d=await r.json();localStorage.setItem('iabet_token',d.access_token);location.href='/'}return <main className="mx-auto max-w-md px-5 py-20"><form onSubmit={submit} className="card p-8"><div className="label text-brand">Área segura</div><h1 className="mt-2 text-3xl font-black">Entrar no IABet</h1><label className="mt-7 block text-sm text-muted">E-mail<input name="email" type="email" required className="mt-2 w-full rounded-xl border border-line bg-ink p-3 text-white"/></label><label className="mt-4 block text-sm text-muted">Senha<input name="password" type="password" required className="mt-2 w-full rounded-xl border border-line bg-ink p-3 text-white"/></label>{error&&<p className="text-sm text-red-400">{error}</p>}<button className="mt-6 w-full rounded-xl bg-brand p-3 font-bold text-ink">Entrar</button></form></main>}
